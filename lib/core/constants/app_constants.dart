class AppConstants {
  const AppConstants._();

  static const appName = 'CraftEdge HRMS';
  static const hiveAppBox = 'craftedge_app';
  static const authTokenKey = 'auth_token';
  static const defaultPageSize = 20;
}
