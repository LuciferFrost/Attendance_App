class AnalyticsService {
  Future<void> logScreenView(String screenName) => Future.value();

  Future<void> logEvent(String name, {Map<String, Object>? parameters}) {
    return Future.value();
  }
}
